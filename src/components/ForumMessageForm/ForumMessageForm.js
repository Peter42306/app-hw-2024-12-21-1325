import React from 'react';
import PropTypes from 'prop-types';
import './ForumMessageForm.css';

const ForumMessageForm = () => (
  <div className="ForumMessageForm">
    ForumMessageForm Component
  </div>
);

ForumMessageForm.propTypes = {};

ForumMessageForm.defaultProps = {};

export default ForumMessageForm;
